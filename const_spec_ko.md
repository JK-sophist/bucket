# const 게시판 규칙 명세 (테스트 반영본)

## 1. 단어 입력 규칙
- 단어 입력 창에서 `Enter` 키는 **최종 입력(제출) 도구**로 사용한다.
- 입력값은 단어 단위로 **좌/우 트림(trim)** 후 처리한다.
- **대소문자를 구분**한다. (예: `Samsung` 과 `samsung`은 다른 단어)
- 허용 문자: **한글, 영문, 숫자**만 허용한다.
- 단어 구분자:
  - 공백(`space`)
  - 쉼표(`,`)
- 구분자로 분리된 각 단어는 개별적으로 좌/우 트림 후 저장한다.

## 2. 월별 단어 등록권 발급/패널티
- 월별 단어 등록권은 **한국시간(KST)** 기준으로 매월 `00:00:00`에 발급된다.
- 해당 월 등록권을 미사용하면 **다음 1개월 동안 패널티**가 적용된다.

## 3. 활동 기준
- 활동 인정 기준은 **신규 등록글(새 글 작성)**로 제한한다.
- 수정/삭제/댓글 등은 활동으로 인정하지 않는다.

## 4. 게시판 지분 계산
- 게시판 지분은 다음 공식으로 계산한다.

  `내 지분(%) = (내가 소유한 단어 개수 / 게시판 전체 등록 단어 개수) × 100`

- 소수점 처리: **소수점 둘째 자리에서 반올림**한다.
- 예시:
  - 게시판 단어: `국립`, `공주대`, `물리학과` (총 3개)
  - 내 소유 단어: `국립` (1개)
  - 내 지분: `1/3 × 100 = 33.3%`

## 5. 단어 대여 권한
- 대여받은 사용자는 대여 기간 동안 해당 단어를 **소유자처럼 사용**할 수 있다.
- 대여 기간이 만료되면, 대여로 부여된 권한은 **즉시 소멸**한다.

## 6. 비고
- 본 문서는 테스트 목적의 정책 반영본이다.
- 기존 권장사항은 충돌하지 않는 범위에서 함께 반영 가능하다.
